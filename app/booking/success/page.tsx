import Link from "next/link"; export default async function BookingSuccessPage({ searchParams }: { searchParams: Promise<{ booking?: string }> }) { const params = await searchParams; return <main className="container" style={{padding:'72px 0'}}><div className="panel"><p className="eyebrow">Request received</p><h1>Thank you</h1><p>Your appointment request has been saved. The studio will confirm the time soon.</p>{params.booking ? <p>Booking number: <strong>{params.booking}</strong></p> : null}<Link className="button" href="/client">Open client center</Link></div></main>; }
