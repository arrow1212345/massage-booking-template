import { revalidatePath } from "next/cache"; import { prisma } from "@/lib/prisma";
async function createRule(formData: FormData) { "use server"; await prisma.availabilityRule.create({ data: { locationId: String(formData.get("locationId")), weekday: Number(formData.get("weekday")), startTime: String(formData.get("startTime")), endTime: String(formData.get("endTime")) } }); revalidatePath("/admin/availability"); }
export default async function AdminAvailabilityPage() { const [locations, rules] = await Promise.all([prisma.location.findMany(), prisma.availabilityRule.findMany({ include: { location: true }, orderBy: [{ weekday: "asc" }, { startTime: "asc" }] })]); return <><h1>Availability</h1><form action={createRule} className="panel form"><select name="locationId">{locations.map(l => <option key={l.id} value={l.id}>{l.name}</option>)}</select><input name="weekday" type="number" min="0" max="6" placeholder="0 Sunday - 6 Saturday" required/><input name="startTime" type="time" required/><input name="endTime" type="time" required/><button>Add rule</button></form><table style={{marginTop:24}}><tbody>{rules.map(r => <tr key={r.id}><td>{r.location.name}</td><td>Weekday {r.weekday}</td><td>{r.startTime} - {r.endTime}</td></tr>)}</tbody></table></>; }
