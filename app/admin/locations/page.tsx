import { revalidatePath } from "next/cache"; import { prisma } from "@/lib/prisma";
async function createLocation(formData: FormData) { "use server"; await prisma.location.create({ data: { name: String(formData.get("name")), address: String(formData.get("address")), description: String(formData.get("description") || ""), requiresCustomAddress: formData.get("requiresCustomAddress") === "on" } }); revalidatePath("/admin/locations"); }
export default async function AdminLocationsPage() { const locations = await prisma.location.findMany({ orderBy: { name: "asc" } }); return <><h1>Locations</h1><form action={createLocation} className="panel form"><input name="name" placeholder="Location name" required/><input name="address" placeholder="Address" required/><textarea name="description" placeholder="Description"/><label><input type="checkbox" name="requiresCustomAddress"/> Client must enter custom address</label><button>Add location</button></form><div className="grid two" style={{marginTop:24}}>{locations.map(l => <div className="card" key={l.id}><h2>{l.name}</h2><p>{l.address}</p><p>{l.requiresCustomAddress ? "Custom address required" : "Fixed location"}</p></div>)}</div></>; }
