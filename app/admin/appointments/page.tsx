import { AppointmentStatus } from "@prisma/client"; import { revalidatePath } from "next/cache"; import { prisma } from "@/lib/prisma"; import { formatDateTime, formatMoney } from "@/lib/format"; import { sendEmail } from "@/lib/email"; import { createCalendarEvent } from "@/lib/calendar";
async function updateStatus(formData: FormData) { "use server"; const id = String(formData.get("id")); const status = String(formData.get("status")) as AppointmentStatus; const appointment = await prisma.appointment.update({ where: { id }, data: { status }, include: { service: true } }); if (status === "CONFIRMED") { const eventId = await createCalendarEvent(`${appointment.service.name} - ${appointment.clientName}`, appointment.startsAt, appointment.endsAt); await prisma.appointment.update({ where: { id }, data: { googleCalendarEventId: eventId } }); await sendEmail(appointment.clientEmail, "Appointment confirmed", `Your appointment ${appointment.bookingNumber} is confirmed for ${formatDateTime(appointment.startsAt)}.`); } if (status === "CANCELLED") await sendEmail(appointment.clientEmail, "Appointment cancelled", `Your appointment ${appointment.bookingNumber} has been cancelled.`); revalidatePath("/admin/appointments"); }
export default async function AdminAppointmentsPage() { const appointments = await prisma.appointment.findMany({ include: { service: true, location: true }, orderBy: { startsAt: "desc" }, take: 100 }); return <><h1>Appointments</h1><table><thead><tr><th>Booking</th><th>Client</th><th>Service</th><th>Time</th><th>Status</th><th>Action</th></tr></thead><tbody>{appointments.map(a => <tr key={a.id}><td>{a.bookingNumber}</td><td>{a.clientName}<br/>{a.clientEmail}</td><td>{a.service.name}<br/>{a.location.name}<br/>{formatMoney(a.priceCents)}</td><td>{formatDateTime(a.startsAt)}</td><td>{a.status}</td><td><form action={updateStatus} className="form"><input type="hidden" name="id" value={a.id}/><select name="status" defaultValue={a.status}>{Object.values(AppointmentStatus).map(status => <option key={status} value={status}>{status}</option>)}</select><button>Update</button></form></td></tr>)}</tbody></table></>; }
